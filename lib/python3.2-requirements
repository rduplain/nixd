#!/bin/bash

REQUIREMENTS=$NIXD_ETC/python3.2-requirements.txt
PIP=$NIXD_OPT/python3.2/bin/pip

check() {
    nixd_newer_than python3.2 $REQUIREMENTS
}

pretest() {
    nixd_ls $REQUIREMENTS
}

install() {
    $PIP install -r $REQUIREMENTS
}

nixd_run "$@"
